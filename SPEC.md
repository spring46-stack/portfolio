# SPEC — ポートフォリオサイト仕様書

## 1. 目的

自己学習で作成した4つのWebアプリケーション(タスク帳・メモ帳・家計簿・PHPテスト練習)の
制作プロセスをまとめ、今後のキャリアビジョンとあわせて公開するための1ページ型ポートフォリオサイト。

## 2. 想定読者

- 転職・就職活動で本サイトを見る採用担当者・エンジニア
- 自分自身の学習記録の振り返り用

## 3. サイト構成

| セクション | 内容 |
|---|---|
| Hero | 自己紹介、使用技術の要約、CTA(制作物へのリンク) |
| Projects(制作物) | 4プロジェクトをカード表示。クリックで「プロセス」「画面図(プレースホルダー)」「リポジトリリンク」をモーダル表示 |
| Skills(スキル) | 使用技術一覧(PHP, React, Vue.js, JavaScript, MySQL, Laravel, Docker, GitHub Actions) |
| Roadmap(キャリアビジョン) | 1年後/3年後/5年後/10年後の目標をタイムライン表示 |
| Footer | GitHubへのリンク、著作表記 |

## 4. 技術構成

- フロントエンド: React 18 + Vite
- スタイル: 素のCSS(CSS変数によるデザイントークン管理)、ライブラリ非依存
- ホスティング: GitHub Pages(静的サイト)
- ローカル開発環境: Docker / docker-compose(本番のホスティングには不使用。開発環境の統一と、公開前のビルド確認が目的)
- CI/CD: GitHub Actions
  - push / PR時: `npm run build` によるビルド検証 + `docker build` による Dockerfile健全性チェック
  - mainブランチへのpush時のみ: GitHub Pagesへ自動デプロイ

## 5. 非機能要件

- レスポンシブ対応(モバイル幅まで崩れないこと)
- キーボード操作でのフォーカス可視化(アクセシビリティ)
- `prefers-reduced-motion` 設定時はアニメーションを無効化

## 6. 既知の制約・TODO

- `php-testing-practice` リポジトリの詳細内容は未確認。公開前に本人が「プロセス」欄の内容を実際の作業内容に合わせて修正すること。
- 各プロジェクトの画面図は、実際のアプリのスクリーンショットではなくプレースホルダー画像(SVG)。`src/assets/placeholders/` を実際のスクリーンショットに差し替えること。
- Docker環境は本セッションの作業環境では`docker build`の実行確認ができていない(Docker未インストールのサンドボックス環境のため)。ローカルで一度動作確認すること。
